// Test 16: Different parameter names
fn helper(msg, ln, col) {
    seeAm msg;
}

fn test(exp, act, ln, col) {
    if exp == act {
        seeAm "match";
    } else {
        helper("error", ln, col);
    }
}

test("a", "b", 1, 2);
