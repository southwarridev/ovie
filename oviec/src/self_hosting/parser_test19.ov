// Test 19: Workaround with local variable
fn test(a, b) {
    let matches = 0;
    if a == b {
        matches = 1;
    }
    
    if matches == 1 {
        seeAm "equal";
    } else {
        seeAm "not equal";
    }
}

test(1, 2);
test(1, 1);
